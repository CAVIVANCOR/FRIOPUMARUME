// En un archivo donde configures el tema de Material-UI, por ejemplo, theme.js

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Catamaran, sans-serif', // Establecer Catamaran como la fuente predeterminada
  },
});

export default theme;